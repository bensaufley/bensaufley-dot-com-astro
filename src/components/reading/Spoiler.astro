---
import type { JSX } from 'astro/jsx-runtime';

import SpoilerTSX from './Spoiler/index';

export interface Props {
  hide?: boolean;
  children: JSX.Children;
}

const { hide = true } = Astro.props;
---

<SpoilerTSX client:load hide={hide}><slot /></SpoilerTSX>
